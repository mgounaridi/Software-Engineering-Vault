# Native API Tester & Manual JSON Parser
A lightweight Java application that connects to a live remote server, measures network latency, and extracts specific data from a JSON response without relying on third-party libraries.
